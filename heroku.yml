setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE

build:
  docker:
    web: Dockerfile.web
    client: ./client/Dockerfile

run:
  web: rails server -b 0.0.0.0 -p ${PORT:-3000}
  client: npm start